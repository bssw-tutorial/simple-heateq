@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
#
# Examples showing how to re-resolve heateq's dependencies
# during the build of a downstream package when it runs "find_package(heateq)"
#
#find_dependency(MPI 2.0 REQUIRED)
#pkg_check_modules(FFTW3 fftw3 REQUIRED IMPORTED_TARGET)

include ( "${CMAKE_CURRENT_LIST_DIR}/heateqTargets.cmake" )

check_required_components(heateq)
